# Available Resources

Network access is disabled. Use only the data, model, Python packages, and system tools already present in the task environment. Do not add external checkpoints, datasets, labels, or answer mappings.

The coding agent's own model connection is a Harbor phase-scoped exception; it
does not make that model or any public host available to the training process.
