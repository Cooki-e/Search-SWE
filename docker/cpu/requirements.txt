# CPU-only PyTorch runtime. The Dockerfile installs these exact builds from the
# official PyTorch CPU wheel index to avoid pulling CUDA runtime libraries.
torch==2.9.0+cpu
torchvision==0.24.0+cpu

# Model inference, tokenization, and image processing
transformers==5.15.1
Pillow==10.4.0
tiktoken==0.14.0

# Video and media decoding
av==18.1.0
torchcodec==0.9.1
imageio==2.37.4
imageio-ffmpeg==0.6.0

# Retrieval, embedding, and reranking
sentence-transformers==6.0.0
FlagEmbedding==1.4.2
deepspeed==0.19.5
bm25s[full]==0.3.10
rank-bm25==0.2.2
pyserini==1.4.0

# Vector indexing and vector database clients
hnswlib==0.8.0
qdrant-client==1.19.0

# Document parsing, OCR, and rendering
docling==2.121.0
marker-pdf==2.0.0
pdf2image==1.17.0
pypdfium2==5.10.1
CairoSVG==2.9.0

# API clients and HTTP networking
openai==2.54.0
requests==2.34.2
aiohttp==3.14.3

# Service deployment and configuration
fastapi==0.141.1
uvicorn[standard]==0.52.4
python-multipart==0.0.32
pydantic-settings==2.15.0
